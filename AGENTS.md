# AGENTS.md

OntoFlow is a local workbench: Actions wired into workflow graphs, each run getting its own workspace directory and its own DeepSeek Harness subprocess, each Action a session inside it. Read [CONTEXT.md](CONTEXT.md) before naming anything.

## Stance: no compatibility layers

- Do not preserve backward compatibility. Remove obsolete paths instead of adding compatibility layers, fallbacks, or migrations.
- Nothing outside this machine consumes OntoFlow, and there is no migration history to honor: `drizzle-kit push` applies `src/db/schema.ts` in place and no migration files are committed.
- Deleting a subsystem outright is the house precedent — [ADR-0005](docs/adr/0005-folders-not-tags.md) removed the tag tables, API, and components with no alias layer.

## Repository layout

```
src/app/          Next 16 App Router: every page and every REST route; no middleware, no auth
  api/              36 route handlers, each `export const dynamic = "force-dynamic"`
  workflows/[id]/   xyflow canvas editor; editor.tsx (1.2k lines) is the largest file here. It
                    only edits and launches: a started run goes to /runs/<id> (ADR-0018), the
                    canvas has no run bar, no switcher and no ?runId= deep link;
                    settings/ is the workflow-settings page (instructions, toggles, MCP subset,
                    skill set, Tool set — the middle tier of ADR-0016)
  runs/[id]/        the one place a run is watched: read-only canvas over the frozen runs.graph,
                    one time cursor (visuals-at.ts) driving nodes/edges, the round timeline and
                    the per-node drawer (trajectory / inputs-outputs / snapshot)
  monitor/          the system-health page: engine, database, disk, orphan runs and
                    entities, plus the manual cleanup panel — the whole of `/monitor`
src/instrumentation.ts   boot hook; the only server entry point that is not a route
src/rules.test.ts        the mechanically checkable rules of this file as vitest assertions (see Checks)
src/components/library/  list/folder/reference/revision UI the five library pages reuse
src/components/canvas/   flow-node.tsx / flow-edge.tsx plus the node model and the run-visuals
                         context both the editor and the run page render the graph through
src/db/           Drizzle schema + better-sqlite3 singleton (WAL, foreign_keys=ON)
src/lib/          pure, DB-free: graph.ts (validate/back-edge/exits), run-graph.ts (the shape
                  frozen into runs.graph + buildRunGraph/parseRunGraph), values.ts, http.ts, resume-match.ts,
                  workflow-settings.ts (toggle keys, effective-toggle merge, RunSettingsSnapshot),
                  tool-digest.ts (canonical JSON + Tool contract sha256)
src/server/       server-only services; client code imports no value from here
  engine/           runner.ts (orchestration/cancel), action.ts (one node), events.ts (session
                    events → run_events/node_usage as they arrive), rounds.ts (the only writer of
                    run_node_rounds), reconcile.ts (startup)
  harness/          DeepSeek Harness absorption: composition.ts (the per-run cordis.yml, an explicit
                    flat list), catalog.ts (PLUGIN_CATALOG: every upstream row's decision, group,
                    toggle and customization), workspace.ts + launch.ts (run directory, subprocess
                    + stdio JSON-RPC), trajectory.ts, tool-contract.ts (the ctx surface a Tool
                    author sees) + tool-plugin.ts (the cordis wrapper a Tool is materialized into),
                    and rpc/ (the cordis plugin the subprocess loads). catalog.test.ts pins
                    catalog ↔ composition ↔ docs/harness; composition-boot.test.ts really boots
                    the default composition, and one carrying a sample contract Tool, in a
                    subprocess. The @deepseek-ai closure is imported only here
  writers/          per-library write<Kind>() + registry + list.ts query contract + used-by guard
  monitor/          health.ts + disk.ts read-only aggregation; cleanup.ts is the one
                    destructive path (deletes run_events and runs rows and data/runs/<id>, and
                    blanks the round rows' payload columns; dryRun previews it)
  folders.ts references.ts revisions.ts resolve.ts fs-safety.ts settings.ts pricing.ts
  skill-library.ts resume-match*.ts run-rounds.ts (where the round row's skeleton /
  payload split is defined once, for both readers)
scripts/seed.ts   idempotent platform baseline only: the three builtin Object Types and the
                  models rows; case content is seed-resume.ts / seed-leetcode.ts
e2e/              12 Playwright specs + helpers.ts (fixture builders + prefix-scoped cleanup)
docs/             DESIGN.md (v1 + engine spec), DESIGN-V2.md (v2 contracts), DESIGN-V3.md (the
                  2026-09 cleanup / run-page / toolchain contract with its progress table),
                  simplifications/ (one evidence-backed record per simplification candidate:
                  proposed / done / rejected; its README owns the rules), adr/, harness/ (the
                  per-row review record of the upstream composition: README + one file per
                  catalog group + its own AGENTS.md with the upstream-upgrade procedure)
.github/          workflows/ci.yml (merge gate), smoke.yml (paid; manual or nightly), claude.yml
                  (@claude review); REVIEW.md (the review checklist); pull_request_template.md
.nvmrc            the Node major CI installs; package.json `engines` pins the same floor
data/             gitignored runtime root: ontoflow.db, runs/, uploads/, samples/, skills/
_reference/       gitignored third-party source, excluded from tsconfig; never import or edit
```

`.data/` is unrelated private residue with no code reference. Do not read or write it.

## Commands

Run everything from the repository root: `data/` resolves from `process.cwd()` in `src/db/index.ts`, `src/server/fs-safety.ts`, and `drizzle.config.ts`, so another working directory is another database. No external service has to be running — the engine is the `@deepseek-ai` npm closure booted inside a per-run subprocess ([ADR-0006](docs/adr/0006-deepseek-harness-as-execution-engine.md)). What must exist is the model credential: put `DEEPSEEK_API_KEY` in `.env.local` (gitignored) and export it before any command that runs a model. Credentials travel by reference name only: the value is picked from the Next process environment at spawn time and never enters a composition file, a log, or a run directory. Workflows that feed PDF inputs rely on Poppler's `pdfinfo`, `pdftotext`, and `pdftoppm` on `PATH` — Action sessions call them through the built-in `bash` tool ([ADR-0011](docs/adr/0011-bash-in-session-no-platform-preprocessing.md)).

```sh
npm install
npm run db:push     # drizzle-kit push; applies src/db/schema.ts in place, no migration files
npm run db:seed     # tsx scripts/seed.ts; idempotent; platform baseline only: the three builtin
                    # Object Types (text / file / json) and the models rows
npm run dev         # next dev -p 3592
npm run build       # next build; catches route and config breakage typecheck misses
npm run typecheck   # tsc --noEmit under strict
npm run lint        # oxlint with oxlint-tsgolint type-aware rules; .oxlintrc.json owns the rule set (ADR-0019)
npm run fmt         # oxfmt over src/ scripts/ e2e/ and the root configs; docs and Markdown are excluded
npm run fmt:check   # the same, check only; CI runs it
npm run knip        # unused files / exports / dependencies as hints for find-simplifications; not a gate yet
npm run check       # typecheck && lint && fmt:check && test; run this before declaring work done
npm test            # vitest run over src/**/*.test.ts and scripts/**/*.test.ts
npm run test:e2e    # playwright over e2e/; workers: 1
npx playwright test e2e/<name>.spec.ts   # one spec
npx vitest run src/rules.test.ts         # AGENTS.md 里能机械核对的约定；改了本文件的规则先跑它
npx vitest run src/server/harness/catalog.test.ts   # 目录 ↔ 组合 ↔ docs/harness 三方钉死；改组合行必跑

# 花钱的冒烟与验收（真实调用模型，需要 DEEPSEEK_API_KEY）
npx tsx scripts/smoke-harness.ts       # 只验子进程：boot、一轮对话、产物、结构化输出、收束
npx tsx scripts/smoke-engine.ts        # 验整条引擎：两 Action 节点的线性工作流经 startRun 跑通
npx tsx scripts/smoke-graph.ts         # 验图能力：扇出、汇总、具名出口、回边重入
npx tsx scripts/smoke-capabilities.ts  # 验能力：技能被发现、工具被调用、停用工具从清单消失
npx tsx scripts/smoke-parallel.ts [并发数]  # 验并行：同一工作流同时 10 个运行全部成功、产物互不串号
npx tsx scripts/seed-resume.ts         # 装入「简历匹配评分」工作流（不花钱）
npx tsx scripts/seed-leetcode.ts       # 装入「LeetCode 解题验收」工作流：解题⇄测试回边循环 + run_python 工具（不花钱）
npx tsx scripts/run-leetcode.ts [并发数]  # 跑 LeetCode 工作流并对定稿脚本做本地独立验收；并发数默认 1
npx tsx scripts/run-resume.ts [data内岗位路径] [data内简历路径]  # 经内部 API 验收简历匹配评分；先保持 npm run dev 运行
```

### Checks

There is no git hook. The gate is GitHub Actions plus the commands above — `tsc`, oxlint with tsgolint type-aware rules, oxfmt, vitest, `next build` — and running them locally before you push is still your responsibility, because CI only tells you after the fact. The lint and format tools are the oxc pair rather than ESLint and Prettier because this repository is on TypeScript 7, whose npm package has no JavaScript API and on which typescript-eslint refuses to run ([ADR-0019](docs/adr/0019-oxc-toolchain-not-eslint.md)); `.oxlintrc.json` carries the reason for every rule it switches off, and `npm run knip` is a hint generator for `find-simplifications`, not a gate, until its false positives are down to zero.

- `.github/workflows/ci.yml` runs on every pull request and on every push to `main`, and its two jobs are the merge gate. `check` is `npm ci → npm run typecheck → npm run lint → npm run fmt:check → npm test → npm run build` (the job keeps its name because branch protection matches on it); `e2e` builds a fresh `data/ontoflow.db` with `db:push` + `db:seed`, runs `npx playwright test` (the Playwright config starts the dev server), and uploads `test-results/` + `playwright-report/` on failure. Neither job has a model credential, so neither spends money. `npm test` includes `src/server/harness/composition-boot.test.ts`, which really boots a harness subprocess on the default composition, once with every default-on toggle off, once more with the search toggle on, and once with a sample contract Tool's generated wrapper included — no model call, no credential; on ubuntu the bash sandbox rung is the npm-distributed `landlock-run`, which the boot loads but never runs a command through — the kernel fence itself is exercised only by the paid smokes.
- `.github/workflows/smoke.yml` is not a gate: `workflow_dispatch` plus one scheduled run a day (`0 20 * * *` UTC) on macOS with `secrets.DEEPSEEK_API_KEY`, running `db:push`, `db:seed`, `smoke-harness`, then `smoke-engine`. It runs on macOS because the bash sandbox's kernel fence has only been exercised under Seatbelt, and it fails at the first step when the secret is missing rather than midway with `MISSING_CREDENTIAL`.
- `.github/workflows/claude.yml` is on-demand review: mention `@claude` in an issue comment, PR comment, or PR review; it needs `secrets.ANTHROPIC_API_KEY` and the Claude GitHub App, reads `.github/REVIEW.md` and then this file before acting, and may run only `npm run check` / `typecheck` / `test` / `build` / `npx vitest run`. `.github/REVIEW.md` is the shared checklist for Codex auto-review, `@claude`, and humans — this file's invariants regrouped by what to look at in a diff, each item naming the sentence here that owns the reason. `.github/pull_request_template.md` makes three sections mandatory: what changed, which commands ran, which REVIEW.md items were self-checked.
- **A convention that can be checked mechanically is a test, not prose.** `src/rules.test.ts` scans source text for the rules in this file: `force-dynamic` on every route, `handle()` and its four exemptions, no `await db.`, the client/server import boundary (`"use client"` files plus the directive-less shared modules under `src/app/` — `api/` excluded — and `src/components/`) and no `"use server"`, `ontoflow`-prefixed `globalThis` keys, `import "@/server/writers"` on restore routes and one registered writer per `EntityKind`, the list-envelope imports, the raw-`sql` allowlist, `LIKE … escape '\'`, exact `@deepseek-ai` pins, and the byte-identical skills trees. Its whitelists assert that today's exemptions are still exemptions, so fixing one means shrinking the list. A rule here changes in the same commit as that test and the matching REVIEW.md line; a rule the code stops obeying is deleted from all three.
- Run `npm run check` (typecheck, lint, fmt:check, vitest) before declaring work done; add `npm run build` when the change touches `src/app/`, `next.config.ts`, or `tsconfig.json`, because `build` catches route and config breakage `typecheck` misses.
- `next.config.ts` carries two pins a dependency change breaks: a new native or server-only package must join `serverExternalPackages`, and the Turbopack `root` pin is what stops Next latching onto a lockfile outside the repository.
- Unit tests are `*.test.ts` under `src/` or `scripts/`; everything under `e2e/` is Playwright, and the two globs do not overlap. `vitest.config.ts` re-declares the `@/*` alias, so a service test that bypasses it cannot resolve `@/db`. The CI `check` job has no `data/` (it never runs `db:push`) and vitest files run in parallel workers, so a unit test swaps an in-memory database onto `globalThis.ontoflowDb` and creates every directory it needs itself (`recursive: true`); none opens `data/ontoflow.db`.
- Unit tests cover pure logic, non-obvious invariants, and the conventions above — from `src/lib/graph.ts` and `src/server/folders.ts` to `catalog.test.ts` and `rules.test.ts`; everything user-visible is covered by Playwright.
- `test:e2e` starts a dev server or attaches to whatever already listens on 3592 (`reuseExistingServer`), so confirm that server was launched from the repository root or you are testing another database. A user-visible change runs its one matching spec, not the suite. The CI `e2e` job runs the whole suite against a database carrying nothing but the platform baseline and **no run history**, and it is green because every spec builds what it asserts on: library specs create their entities through the API in `beforeAll`, `runs.spec.ts` synthesizes its own runs, events and usage and `monitor.spec.ts` its own finished run plus a real workspace directory on disk, both by direct SQLite, and all of them clean up in `afterAll` — so every assertion is against its own fixture or the API payload, never against seed literals or history that only exists locally.

### Test fixtures cost money, but they are reproducible

**`db:seed` writes the platform baseline and nothing else**: the three builtin Object Types (text / file / json) and the `models` rows — no library entities, no folders, no revisions, no workflow, no sample files, and no runs / run_events / node_usage. Case content is a case's own seed script: `scripts/seed-resume.ts` (which also writes the fictional job/résumé samples under `data/samples/`) and `scripts/seed-leetcode.ts`, both as idempotent as `seed.ts` and both dependent on nothing but the baseline. So a fresh `db:push` + `db:seed` is exactly what CI's `e2e` job starts from, and a spec that wants an entity creates it.

- **No test may assert on a seed literal.** There are no seeded library entities left to assert on; each spec builds its own `e2e-`-prefixed fixture and removes it in teardown (see Checks).
- **The run history can be rebuilt.** `scripts/run-resume.ts` and `scripts/run-leetcode.ts` regenerate it from scratch against the current engine, so losing `data/ontoflow.db` costs money and time rather than being unrecoverable. Rebuilding still means paid model calls — treat it as expensive, not as impossible. (This section used to say the fixture could not be regenerated; that was true of the opencode engine, whose runs no longer have any way of being reproduced.)
- E2E never starts a run that contains an Action node (that spends money) and never clicks 执行清理 / 确认删除 / 中止该运行; the cleanup panel is exercised only through `dryRun`. The one sanctioned run-starting shape is `parallel-runs.spec.ts`: an input→output workflow with no Action nodes costs nothing, still exercises the full engine lifecycle, and deletes its runs in teardown via `DELETE /api/runs/[id]`.
- E2E creates entities under a per-spec `e2e-` Chinese prefix and removes them in teardown through `cleanupByPrefix`, which skips `builtin` rows and re-checks the prefix. `settings.spec.ts` is the exception the rule cannot cover: settings are one document rather than named entities, so it saves the whole document in `beforeAll` and writes it back in `afterAll`.
- **Never assert a count, a first-page containment, or an exact row that real usage grows.** Fetch the API payload in the test and assert the DOM matches it. This bug class has now been fixed three times; the third time it was the run-detail spec asserting that the newest run was a particular seeded one.

## Conventions

- User-facing strings, error messages, code comments, and test names are Chinese; identifiers are English.
- Every API route body runs inside `handle()` from `@/lib/http`. Two do not: `api/runs/[id]/events` returns a raw SSE `Response`, and `api/models` is a one-statement GET that predates the rule — do not copy them.
- **Name collisions are a database concern.** `handle()` maps `UNIQUE constraint failed` to 409, so writers never pre-check a name; folders are the exception, because SQLite cannot constrain a root level whose parent is NULL ([reason](src/db/schema.ts)).
- **Entity-body validation lives in the writer's `parse…Payload`** (workflow's is `parseGraphPayload`); a route hand-narrows only its own non-entity params. All of it is hand-written `typeof` narrowing — there is no schema library.
- **Write paths return a result object; the engine throws.** `runner.ts` turns a thrown error into `run_nodes.error` plus skipped downstream nodes. New service modules use `WriteResult` with `writeOk`/`writeFail` from `@/server/writers/types`; `folders.ts` and `revisions.ts` still carry private structurally identical `Result<T>` copies — converge on `WriteResult`, never add a fourth. `fs-safety.ts` and `monitor/cleanup.ts` throw deliberately and their callers map the throw.
- **better-sqlite3 is synchronous.** Drizzle calls end in `.get()` / `.all()` / `.run()`; never `await db.…`.
- Raw SQL goes through drizzle's `sql` tag — both spellings, `sql\`…\`` and `sql<T>\`…\`` — and only where the query builder cannot express the aggregate: `monitor/` (cleanup, health), `writers/list.ts` (the `LIKE` search), `engine/action.ts` (the per-session `SUM` rollup), `engine/events.ts` (the `json_extract` tool-name lookup), `revisions.ts` (`max(version_no)`), and `api/runs/route.ts` (the per-run totals, the filter-set usage summary, and the `json_extract` source projection); `src/rules.test.ts` pins that allowlist and asserts every listed file still needs it. User input inside `LIKE` is escaped and paired with `escape '\'`.
- Process-level mutable state is parked on `globalThis` under an `ontoflow`-prefixed key so HMR cannot lose it: `ontoflowDb`, `ontoflowCancelledRuns`, `ontoflowRunProcesses`, `ontoflowActiveRuns`, the usage-settlement maps, the skill-projection holds; `src/rules.test.ts` pins the prefix on every key. A module-level `const map = new Map()` is the bug this prevents.
- A server-module unit test assigns an in-memory database to `globalThis.ontoflowDb` and then `await import()`s the module under test; a static import reaches the real `data/ontoflow.db`.
- **There are no Server Actions.** Every mutation is a `fetch` to `/api/*`; new pages start with `"use client"` and load data in `useEffect`.
- Client code imports no runtime value from `@/server` or `@/db`. `import type` from `@/server/monitor/types` is the sanctioned exception.
- **Global settings are one JSON document in a single-row table**, validated whole at the write boundary in `src/server/settings.ts` and read once when a run starts — a change takes effect on the next run, and a running run holds the snapshot it started with. Besides the credential refs, MCP servers, and disabled tools, the document carries `toggles` — the global default of every toggleable composition row, five keys (`webSearch` / `fsSearch` / `strReplaceEditor` / `todo` / `compaction`, `DEFAULT_COMPOSITION_TOGGLES` in `src/lib/workflow-settings.ts`, a partial body fills the rest from the defaults, a non-boolean is 400) — and `defaultInstructions`, the text written to `<run>/home/AGENTS.md`, which upstream `agent-instructions` reads as the user-level `$DSH_HOME/AGENTS.md`, so every Action session gets it unconditionally (factory default `DEFAULT_INSTRUCTIONS`: the four runtime rules that used to be hardcoded in `runner.ts`; ≤ 64 KiB, an empty string is legal). The per-run composition gives `agent-instructions` a batch budget of both instruction limits plus a frame allowance (`INSTRUCTIONS_BATCH_MAX_BYTES` in `src/lib/workflow-settings.ts`), because upstream budgets `$DSH_HOME/AGENTS.md` and `workspace/AGENTS.md` together and, when the batch is over, silently omits the user-level file first. A paid script that temporarily changes this document installs and restores it through `replaceSettingsIfCurrent`; a user save made during the run wins instead of being overwritten by the script's stale snapshot. Credentials appear only as environment-variable names; the value is picked from the Next process environment at spawn time. An MCP `env` key whose name looks like a credential is refused, because that object is written verbatim into the run's composition file.
- **Settings have three tiers: the upper tier declares, the lower one narrows, and Action never toggles a plugin** ([ADR-0016](docs/adr/0016-three-tier-settings.md)). The global document is the baseline. A workflow owns `instructions` (written verbatim to `workspace/AGENTS.md`; an empty string becomes `# <workflow name>\n` so the file still exists; ≤ 64 KiB), `settings.toggles` (only the keys it overrides; unknown keys or non-booleans are 400), `settings.mcpServers` (a subset of global registry names by shape `MCP_SERVER_NAME_PATTERN`, not checked against the registry at save time — a name the registry no longer has is silently ignored at admission), a skill set (`workflow_skills`) and a Tool set (`workflow_tools`). An Action owns only what it picks inside those sets: `action_preloads` ⊆ the workflow's skill set and `action_tools` ⊆ its Tool set. Because an Action is still a shared library entity ([ADR-0010](docs/adr/0010-nodes-own-their-definition.md) is unimplemented), the ⊆ rule is enforced where the set is known, not on Action save: `parseGraphPayload` answers 400 naming the Action and the skill/Tool, and `resolveWorkflow` throws `WorkflowResolveError` (status 422, `issues[]`, the same shape as graph validation) at admission — `startRun` and the resume-match endpoint both map it. `workflows/[id]` PUT treats a missing `instructions` / `settings` / `skillIds` / `toolIds` as "keep the current value" (the canvas saves only the graph and must not empty the sets) and a present one as whole replacement. The merge is `effectiveToggles(global, workflow)` and `global-enabled ∩ workflow-subset` for MCP; both land in `runs.settingsSnapshot` (`RunSettingsSnapshot`: `global` / `workflow` / `effective`, instruction texts as sha256) in the same transaction as the `runs` row, which is what the run page's 设置快照 panel and the audit of "why did this run have `web_search`" read.
- **A globally disabled tool disappears from the session's tool list**, it does not fail when called. `tools.restrict` narrows the visible set and `tools.guard` covers tools registered after the session opened; the observable evidence is the tool's absence from `request/header`, not an error event. The deny list is one set: `disabledTools ∪ (the workflow Tool set's public names − this Action's visible public names)` (`toolFilterForAction`); every Tool in the workflow set is materialized and registered globally, and visibility is subtracted per session.
- **The plugin panel reports a derivation, not a live tree.** There is no long-lived harness host inside the Next process — every run boots its own subprocess — so "what is mounted right now" has no answer between runs. `/api/settings/composition` reports the composition the next run would boot from the **global** toggles alone (a workflow's overrides are not folded in — that answer lives in each run's `settingsSnapshot`) plus the last run's `cordis.yml` as it actually landed on disk. It also projects `PLUGIN_CATALOG` as `groups`: the ten `PLUGIN_GROUPS`, each row with its decision, entry id (a fixed id or `<prefix>*`), workflow-toggle flag, customization, and a mount state (`会挂载 / 按开关未挂 / 按开关已挂 / 不挂 / 备选 / 库 / 按运行生成 / 自有` — `按运行生成` is a prefix row, MCP servers or Tool plugins, with no instance in the preview composition; `自有` is a row of this repository's own code that is not a composition entry) derived from the same `runCompositionEntries` call with the current settings' toggles. Client code cannot import from `@/server`, so the catalog reaches the settings page only through this route; the page renders the ten groups (1–4 and 10 open, 5–9 collapsed) and never keeps its own grouping — `e2e/settings.spec.ts` asserts the DOM against the API payload group by group.
- **The five library list GETs and `/api/runs` return `{ items, total, page, pageSize }`** — the library five built from `parseListQuery` + `selectLibraryPage` + `listEnvelope` in `src/server/writers/list.ts`, `/api/runs` assembling its own envelope (plus a `summary` of the whole filter set: `tokens` / `cost` summed from `run_nodes`, the authoritative per-node rollup, and only `byModel` from `node_usage`, which can lag by a chunk whose insert failed and was folded into `run_nodes` from memory) but taking its paging defaults and cap from that module's `parsePageQuery`, so the 30/100 numbers have one home. A run's entry is a read-time projection of `imports.invocation.source` — `/api/runs` derives `items[].source` and its `source=` filter with `json_extract`, coalescing a run with no invocation to `workflow`; the fact is not stored twice. Every other GET defines its own shape.
- The five library pages reuse `src/components/library/`; no page grows its own tree, toolbar, folder picker, or revision panel. Their filter state lives in the URL through `use-library-query`, never in component state.
- Workflows never enter folders; gate every folder path on `isFolderEntityKind`, not an ad-hoc comparison ([ADR-0005](docs/adr/0005-folders-not-tags.md)).
- **Delete protection is per-owner and there are exactly four.** The four referenceable libraries answer 409 through `usedByNames()`; workflow DELETE has its own running-run guard; folder DELETE has its own name-collision guard; run DELETE refuses a running run with 409 through `deleteRun` in `monitor/cleanup.ts` — the destructive path stays in that one module. Do not add a fifth, and do not hand-write a reference join — `src/server/references.ts` is the only module that joins references, and its fact table is: Action ← `workflow_nodes.action_id`, Skill ← `workflow_skills`, Tool ← `workflow_tools`, Object Type ← `action_ports` + `workflow_nodes`. A Skill or Tool is referenced by a **workflow** (detail 「技能集」 / 「Tool 集」, href `/workflows/<id>/settings`), never by an Action: an Action's preloads and visible Tools are a selection inside a set the workflow already references, so they are not references and delete protection does not look at them.
- **Every entity write records a revision inside the same transaction**, capturing the complete definition including relations (an Action's ports, `preloadSkillIds` and `toolIds`; a Skill's `files`; a Tool's whole contract; a workflow's `instructions`, `settings`, `skillIds`, `toolIds`, nodes and edges), and rollback replays the same `write<Kind>()`; a route that can reach revision restore carries `import "@/server/writers";` or restore silently answers 501.
- Untrusted paths pass through `@/server/fs-safety` (`isWithinData` at the request boundary, `resolveWithinData` and `safeBasename` at use); the run subprocess reads and writes whatever is in its workspace.
- Ids are `crypto.randomUUID()` from the schema default; a caller supplies an id only when it must return that id before insert or preserve client-side edge references.
- **Nominal port typing is an edit-time rule, not a TypeScript one and not a runtime one.** Two ports connect only when their `objectTypeId` values are equal ([ADR-0002](docs/adr/0002-nominal-port-typing.md), amended by [ADR-0008](docs/adr/0008-artifacts-not-values.md)); ids stay bare `string`. Nothing checks at runtime that the artifact holds what its type claims — the only mechanical backstop is that a declared artifact must exist on disk.
- **The workflow graph is not a DAG.** Fan-out is one output port with several edges, synthesis is one input port with several edges, and an edge from an exit port back to an upstream Action is a legal cycle ([ADR-0009](docs/adr/0009-exit-ports-and-back-edges.md)). `classifyEdges` decides which edges are back edges by a stable DFS from the entry nodes, and that answer is load-bearing in three places: readiness ignores back edges (a node that waits on one deadlocks its own loop on the first pass), `downstreamOf` ignores them (or the closure swallows the whole cycle), and validation requires every back edge's target to declare a re-entry limit.
- **Every output port declares an artifact path, and a branching Action reports its exit.** Ports sharing an `exitName` form one exit; the data-plane result names the exit taken and only that exit's edges activate, the rest go dead and their downstream is skipped. Re-entry bumps the whole loop body's round together — each node to **its own** next unused number, never to "the re-entered node's round + 1", or nested loops push a node back onto a round it already used and collide on the round row's unique key — and round N writes under `rounds/N/` so an earlier round is never overwritten. Every execution of every node is one `run_node_rounds` row, input, output and skipped nodes included ([ADR-0018](docs/adr/0018-run-page-frozen-graph-replay.md)); `run_nodes` stays the node's latest-state row and no longer carries the round history.
- **A synthesis port waits for every incoming edge to settle, not for the first one.** Readiness on a port with several edges means all of them are satisfied-or-dead and at least one is satisfied. Treating "any edge satisfied" as ready lets a synthesis node start before its siblings finish and silently read a subset — observed as a six-critic report that only found five verdicts while all six were on disk.
- **A run with output nodes but none reached is a failure, not a success.** An unreached output node on an untaken branch is normal; all of them unreached means the run produced nothing.
- **A specialized invocation's durable business result lives in `run_results`, not in its cleanup-eligible workspace or event log.** A completion gate verifies the final artifact, then the engine writes its exact content and completion evidence in one transaction before marking the run successful. Workspace/event cleanup keeps it readable; deleting the run cascades it. Never put the full result in `runs.imports`, because run-list/detail APIs expose that metadata.
- **A round row has a skeleton and a payload; the wire carries the skeleton and event cleanup takes only the payload.** The skeleton — round, session, start/finish, terminal state, exit, error — is tens of bytes and is what the replay degrades to once the events are gone. `/api/runs/[id]` and every SSE `snapshot` frame carry **only** the skeleton (`listRoundSkeletons` in `src/server/run-rounds.ts` selects those columns; it never fetches the payload and drops it), and the drawer asks for one round's `{ inputs, outputs, snapshot }` at a time through `GET /api/runs/[id]/nodes/[nodeId]/rounds/[round]` when its inputs-outputs or snapshot tab opens or the cursor moves to another round — a snapshot carries the whole rendered prompt and every preloaded skill's body, and a looping run copies it per round, so hanging it off a frame that repeats every 500 ms pushes the same large object at the page over and over. `cleanup.ts`'s `events` target deletes `run_events` and nulls `inputs` / `outputs` / `snapshot` on `run_node_rounds` **and** `run_nodes` rows — the three columns on `run_nodes` are a copy of the latest round and would otherwise still go out whole through `/api/runs/[id]` — never the rows themselves; only the `runs` target (and `deleteRun`) removes them, by cascade. Blanking eligibility is per **run** (terminal, and `finished_at` older than the cutoff), never "this run has old event rows": a free input→output run writes no `run_events` at all, and so does an Action that fails before its first event, so deriving the run set from events would leave their payloads in the database forever and invisible in the preview. Both the preview and the real pass read the blanked-row counts from the same queries, which is what `dryRun` promises. A blanked column comes back as `null`, the same shape as "this round never had one", so the drawer explains both with one sentence; a blanked snapshot costs the trajectory panel its skill-name mapping and it falls back to slugs, which is the accepted price.
- **A specialized paid invocation pins behavior, not names.** Before admission it compares three digests with the reviewed seed pins in `src/lib/resume-match.ts`: (1) the workflow behavior digest — sha256 of the canonical JSON of `{ instructions, settings.toggles, settings.mcpServers (sorted), skill-set names (sorted), Tool-set public names (sorted) }` (`RESUME_MATCH_WORKFLOW_BEHAVIOR_SHA256`); (2) every participating Action's `{ name, prompt, rule, providerId, modelId, reasoningEffort, maxReentries, onExhausted, preloadSkillNames (sorted), toolPublicNames (sorted) }` (`RESUME_MATCH_ACTION_BEHAVIOR_SHA256`); (3) the validator Tool's contract digest `toolContractSha256({ publicName, description, parameters, output, timeoutMs, code })` from `src/lib/tool-digest.ts` (canonical JSON sorts object keys, so re-ordering a schema's keys is not a change). The workflow description and a Tool's display name are not part of any contract. Exact ports and topology alone cannot prove that an editable shared Action still performs the promised business task. The seed scripts throw and print old/new values when a pin no longer matches; re-pinning is an explicit review step whose PR description lists both values.
- **A canvas node is a reference to the shared Action**, so editing it from the canvas edits that Action everywhere ([ADR-0004](docs/adr/0004-canvas-edits-the-shared-action.md)).
- **A run freezes executable definitions at admission.** `resolveWorkflow` captures the graph's Object Types, complete Action/model/port definitions, the workflow's `instructions` and normalized `settings`, its skill set (`ResolvedSkillRef { id, name, slug }`) and full Tool rows, each Action's preloads (`ResolvedActionDefinition.preloads`) and visible Tool public names (`toolNamesByActionId`) in one event-loop turn; `startResolvedRun`, capability materialization, and every later Action node consume that same object instead of querying the shared libraries again. Editing a shared Action, Tool, or workflow setting after admission affects the next run, not an already admitted paid run. The canvas graph is frozen the same way: `buildRunGraph(resolved)` goes into `runs.graph` in the same transaction as the `runs` row, and the run page draws only that ([ADR-0018](docs/adr/0018-run-page-frozen-graph-replay.md)) — a run outlives the next edit of the workflow, so following it on the live canvas is only ever right for a run still in flight. Runs admitted before that column get the default empty graph and render through the same path; there is no legacy branch. Skill directories remain the intentional live-symlink exception described below.
- Input and output node ports are always named `"value"`.
- **A Tool is an OntoFlow contract; the cordis wrapper belongs to the platform** ([ADR-0017](docs/adr/0017-tool-is-a-contract.md)). A `tools` row is `{ name (display, Chinese), publicName (`^[a-z][a-z0-9_]{0,63}$`, unique, and not one of `TOOL_RESERVED_PUBLIC_NAMES` in `tool-contract.ts` — the upstream built-in tool names plus `structured_output` / `run_code` / `web_fetch`, because a wrapper registering an upstream name fails the whole boot and a session-level `structured_output` would be shadowed; nor prefixed `mcp__`, the MCP client's public-name namespace, where a collision drops that whole server's tool list at sync time — the name the model calls), description, parameters (object-root JSON Schema), output? (same, or null), timeoutMs? (positive integer or null), code }`, where `code` is an execute module: `export default async function execute(args, ctx: ToolContext)`. `ctx` is the small stable surface in `src/server/harness/tool-contract.ts` — `workspaceDir` / `runDir` / `tmpDir` / `dataDir` / `dbPath` (absolute), `cwd` (the calling session's), `env` (only the whitelisted names: credential refs, the model credential, `ONTOFLOW_DB_PATH` / `ONTOFLOW_DATA_DIR`), `signal`, and `run(command, { cwd?, timeoutMs? })` — and never a cordis Context. `code` may import `node:` builtins and this repository's dependencies (module resolution walks up from the run directory) but must not import `@deepseek-ai/*`; the writer rejects that at save time (its inline `FORBIDDEN_IMPORT` check; `toolCodeProblem` in `src/app/tools/tool-form.ts` is the client mirror) because it is a bare plugin in disguise. At admission `materializeToolPlugins` writes every Tool of the workflow's Tool set to `<run>/plugins/` as two files — `tool-<id>.execute.ts` (the code verbatim) and `tool-<id>.ts` (the generated wrapper: `name` / `inject = ["tools", "shell", "sandboxPolicy"]` / `apply` registering `publicName`, the schemas, `timeoutMs` only when non-null, `output.schema` `{}` when the contract declares none, and `render: (_args, value) => [{ type: "text", text: JSON.stringify(value) }]`) — and includes each wrapper by absolute path. Only `src/server/harness/tool-plugin.ts` knows the upstream registration shape; an upstream API drift is fixed there once, and `composition-boot.test.ts` really boots a composition carrying a sample contract Tool. `ctx.run()` goes through the same `workspace-write` sandbox as `bash` (`sandboxPolicy.resolve({ session })` + `ctx.shell.run`, default 60 s, capped at 120 s) and returns `{ stdout, stderr, exitCode, timedOut, sandbox: { mode, enforced, runnerFailed, denied } }`; a `SandboxUnavailableError` is folded into `runnerFailed: true, enforced: false, exitCode: null` rather than thrown, so a careful Tool gates on `enforced && !runnerFailed` (`run_python` does). File and network effects a Tool makes with Node APIs directly are not fenced by anything. External capability still goes through MCP, not through a Tool.
- **The JSON Schema subset is narrower than JSON Schema, and the write boundary is the gate.** A type array (`type: ["integer", "null"]`) is rejected by `objectSchemaProblem` in `src/server/writers/json-schema.ts` (mirrored client-side in `src/app/tools/tool-form.ts`) for both `parameters` and `output`, so it never reaches a run; omit the field instead of typing it nullable. Upstream `ctx.tools.register` validates only `output.schema`, not `parameters` — a bad parameter schema that slipped past the writer would surface at the model's first call, not at plugin load — which is why the writer, not the loader, owns this rule. The same subset governs an Action's data-plane schema, which `composeNodeScope` asserts at session creation.
- **A Skill is a directory owned by a workflow's skill set, projected to disk, and preloaded by the upstream slash gesture** ([ADR-0016](docs/adr/0016-three-tier-settings.md)). A `skills` row is `SKILL.md`'s body plus `skill_files` (path + bytes; ≤ 32 files, ≤ 1 MiB each, relative `/`-separated paths with no `.`/`..`/empty segments, no control characters, ≤ 200 chars, `SKILL.md` not allowed as a file nor as a top-level directory name, a path cannot be both a file and another file's directory, and duplicates and file/directory conflicts are compared after NFC + case folding because APFS is insensitive to both); PUT replaces the whole file list, so the editor submits it whole. `src/server/skill-library.ts` writes each Skill to `data/skills/<slug>/SKILL.md` plus `data/skills/<slug>/<path>`, writing the new version to `data/skills/.versions/<slug>-<stamp>/` and then swapping `data/skills/<slug>` — a symlink — onto it with one `rename`, so the path never has an instant in which it does not exist, and `createRunWorkspace` digests a skill through its real path so a rewrite during admission cannot mix two versions; the previous version directory is removed at once, or after the last admitted run holding the slug settles. A run symlinks the workflow's whole skill set into `workspace/.agents/skills/`, so every Action in the run sees every skill's name and description through `skill-filesystem` and loads what it judges relevant. Admission synchronously verifies and holds every projection until that run and subprocess fully settle; deleting an unlinked Skill removes its database row immediately but defers directory removal while any admitted run holds it, and a new admission cannot extend a pending deletion. A preload is not injection: `buildPrompt` puts one `/<slug>` line per preloaded skill before the task text (blank line between), and upstream `tool-skill`'s `agent/pre-step` listener expands the gesture into a `<skill_content>` block with source `skill-invocation` in the same step — exactly what a person typing `/skill-name` in the CLI gets; the trajectory panel labels it 「预载技能：<name>」. Preload costs the whole `SKILL.md` body in the session's first message, so the editors show `estimateTokens` next to each choice. Anything that must apply to every Action belongs in the workflow's `instructions` (`workspace/AGENTS.md`) or the global `defaultInstructions`.
- **The skill directory name is an id-stable ASCII slug, never the library name.** Upstream enforces `/^[a-z0-9]+(?:-[a-z0-9]+)*$/` on skill names, and this repository names entities in Chinese — a Chinese name is dropped with nothing but a `warn` in cordis's in-memory log buffer (no exporter writes it to a file), so the skill silently never reaches the model. `skillSlug()` hashes the entity id so an active run's symlink survives a rename; the same slug is the frontmatter `name` and the name the preload gesture `/<slug>` invokes, while the current Chinese display name is prefixed onto the description the model actually matches on.
- A `models` row's `providerId` is a **dsh provider route**, not a vendor name: `llm-deepseek` registers exactly `deepseek-official`. Adding a model means `upsertModel` in `scripts/seed.ts` plus, for a route no adapter serves yet, a new entry in `runCompositionEntries`. No route writes `models`.

### The harness seam

Every rule here was learned the hard way while replacing opencode with dsh. Read the header comments of `src/server/harness/runtime.ts`, `src/server/harness/rpc/server.ts` and `src/server/engine/action.ts` before touching sessions, events, usage, or cancellation. A change on this seam also runs at least `smoke-harness`, or the PR says why it can skip it: CI has no credential, so only a paid smoke exercises a real model call.

- **One run, one subprocess, one workspace.** `executeRun` creates the run directory, spawns `node --import tsx src/server/harness/runner.ts <cordis.yml>`, and disposes it in a `finally`. There is no build step for server code: the subprocess loads the same TypeScript the Next process does.
- **Runs execute in parallel and are mutually independent.** Nothing serializes runs: cross-run state is keyed by runId on `globalThis` or lives inside the run's own directory, and anything new must follow suit. `startRun` is the only admission gate — at `MAX_CONCURRENT_RUNS` simultaneously running runs it answers 429 instead of queueing, because each run is a whole node+tsx+dsh subprocess; the external caller owns any queue.
- **The RPC plugin is named in the composition by absolute path, not by package name.** The cordis loader resolves bare specifiers from its own location inside `node_modules`, and this repository has no self-referencing symlink there, so a bare name fails at boot. `boot`'s `bareModuleBaseUrl` does not rescue it.
- **Pin every user-level root inside the run directory.** `skill-filesystem` defaults `agentsHome` to `~/.agents`, so a run will happily discover and load the machine owner's personal skills — observed in practice as an agent looping through unrelated skills and then failing to read their resources. The per-run composition pins both `dshHome` and `agentsHome` under the run's home directory; workspace isolation is not automatic. The one file that is meant to live in that home is `<run>/home/AGENTS.md`, written from the global `defaultInstructions` (`createRunWorkspace({ homeInstructions })`) — the machine owner's `~/.dsh/AGENTS.md` never reaches a run.
- **`TMPDIR` is pinned to `<run>/tmp` at spawn, and the spill root to `<run>/home/spill`.** Upstream `sandbox-local` lets `workspace-write` write to `os.tmpdir()`, which honours `TMPDIR`, so `launchRun` injects `TMPDIR=<run>/tmp` next to `DSH_HOME`: bash `mktemp`, Python `tempfile`, Poppler scratch files, `subprocess-local`'s full-output files and the sandbox's own temp root all land inside the run directory, go with it on cleanup, and count in the disk figures; unpinned, they scatter under `/var/folders`. `<run>/tmp` is a sibling of `workspace/`, never inside it. `spill-local` without `root` writes to a process-private directory under `os.tmpdir()`; the composition pins `root` explicitly under `home/` rather than the workspace, because spill files are not artifacts and must not be swept up by `glob` or read by a downstream node as an upstream product.
- **Every composition row is decided in three places, and `catalog.test.ts` keeps them agreeing** ([ADR-0013](docs/adr/0013-explicit-composition-headless-baseline.md)). `catalog.ts` (`PLUGIN_CATALOG`: decision, group, entry id, `mountedByDefault`, `workflowToggle`, one-line reason, customization) is the declaration, `composition.ts` is the executable list, and `docs/harness/NN-*.md` is the argued reason with the package string verbatim. The test asserts the default composition's entries are exactly the rows marked default-mounted, that `mountedByDefault: false` rows appear only under their toggle, that every package string appears in its group's file, and that `docs/harness/README.md`'s upstream version equals `UPSTREAM_VERSION`, the `package.json` pins, and every customization's `upstream.version`. Adding, removing, or re-deciding a row is all three files in one change, then `npm run check`; `composition-boot.test.ts` then really boots it. The test also requires every entry of the preview composition to be a fixed id the catalog names — `catalogRowForEntryId` matches exact ids before prefix rows, so a `tool-*` / `mcp-*` entry cannot hide behind the Tool-plugin or MCP prefix row — and each package string to appear as a whole word in its group's document. Three config keys are silently load-bearing: `spill-policy.maxInlineBytes` omitted means the policy is off, not defaulted; `tool-todo.allowParallelInProgress` and `tool-fs-search.sampleOverCapGlobResults` are required, and omitting either fails plugin load and takes the whole run down — the boot test is what catches that before a paid run does.
- **Compaction summary usage arrives on `compaction/summary`, not as a usage chunk.** `compaction-basic` makes the summarize call straight through `ctx.llm.stream()`, never through agent-loop, so the only accounting for it is that event's `usage`; `events.ts` bills it as `node_usage` row `compaction:<seq>` (session-unique, never collides with `turnN-stepM`, idempotent on replay) with variant `compaction` and the event's own provider/model, and the per-session `SUM` folds it into `run_nodes.cost` — the summary is not off the books, except when its commit phase fails: upstream then emits only an errored `compaction/end` and never `compaction/summary`, so that one paid call cannot be billed. `run_events` has a sixth type, `compaction` (`start` / `summary` / errored `end` / `prune`); a `tool/result` whose `surfaceOp.op` is `replace` is a pruned copy of a result already logged, so it is skipped or the call shows two completions.
- **DeepSeek search is off by default because it is off the books.** `web-search-deepseek` is a separate helper model request whose usage never passes `llm/stream` (the upstream source has no usage field), so `node_usage` and the cost page cannot see it: the search trio (`web` / `web-search-deepseek` / `tool-web`, search only, no fetch) mounts only when the effective `toggles.webSearch` is on — the global default is `false` and a workflow may override it in its own settings — and both the global settings page and the workflow settings page say so next to the switch. The other four toggles (`fsSearch`, `strReplaceEditor`, `todo`, `compaction`) default on; only rows the catalog marks `workflowToggle` can be overridden, and `catalog.test.ts` checks every key mounts exactly its rows.
- **Cap the steps of a node.** Upstream `agent-loop` has no step limit, so a looping agent burns tokens until the node's wall-clock timeout. Each session registers an `agent/pre-step` guard from `NODE_MAX_STEPS`; exceeding it ends the turn with no structured output, which fails the node.
- **stdout belongs to the protocol.** The per-run composition must never include a stdout logger; harness stderr goes to `<run>/logs/harness.stderr.log`.
- **Every Action session has `bash`, and every run input is materialized as a workspace file.** The composition mounts the upstream shell chain (`dsh-tool-bash` + `dsh-bash-sandbox` + `dsh-subprocess-local` + `dsh-shell-env`); `bash` is an unscoped built-in like `read`/`write`, and format conversion of inputs happens inside the Action session, never in the Next process ([ADR-0011](docs/adr/0011-bash-in-session-no-platform-preprocessing.md)). Files are copied verbatim; text/json inputs become `inputs/<nodeId>/<label>.md|.json`; a name that would be an upstream instruction file (`AGENTS` / `CLAUDE`, with or without `.local`) is prefixed `输入-` whether it comes from the node label or the uploaded file's name, so an input is never read as that directory's instructions; and prompts reference paths only — `describeInput` throws on an inlined value rather than resurrect the silent-truncation path ([ADR-0012](docs/adr/0012-inputs-materialize-as-files.md)).
- **One sandbox policy covers bash and write/edit, at two different strengths.** `dsh-sandbox-policy` at `workspace-write` confines file writes of both families to the run workspace plus the system temp dirs, but only bash gets a kernel fence: its argv is wrapped by Seatbelt (`sandbox-exec`) and fails closed when the runner is unavailable, while write/edit go through `dsh-fs-sandbox`'s in-process path check — upstream calls it a policy fence, not a kernel boundary, and it never touches the runner. Reads and network are not confined for either family, and `dsh-user-approval` at `policy: "never"` auto-rejects escalation requests.
- **Pin `@deepseek-ai` versions exactly.** The npm dist-tag `latest` on these packages is stale; every dependency is pinned to the closure's version (`0.1.1-rc.2`), and a bare `npm install @deepseek-ai/...` will fetch the wrong generation. Every `@deepseek-ai/dsh-*` package, direct or transitive, also sits in `package.json` `overrides` at that version so an install without the lockfile cannot drift either; `catalog.test.ts` asserts every direct one is there.
- **思考强度 reaches the model only through the `agent/request` waterfall.** `AgentOptions` carries just provider/model/maxTokens, and the `GenerateOptions` handed to `llm/stream` is frozen — assigning to it throws mid-turn. The listener is registered on the session's own scope in `composeNodeScope`, so it is filtered to that agent and released with the session.
- **Overwrite the effort unconditionally.** agent-loop resolves the adapter default into the call config before the waterfall runs, so a "only set it when unset" guard never fires.
- **Structured output is a scope-registered tool, not a provider feature.** Each session registers its own `structured_output` with the real schema; two-phase commit on `tools/result` is what makes the captured value authoritative.
- Token usage arrives as one `usage` chunk per step and does not accumulate, so summing is correct — unlike opencode's repeated `message.updated`, there is no double-count hazard here. `outputTokens` already includes reasoning (the adapter takes `completion_tokens` verbatim), so never bill or total reasoning as an extra bucket.
- **Costs are CNY, computed at write time.** `src/server/pricing.ts` carries the official DeepSeek peak/off-peak price table; `node_usage.cost` prices each usage chunk by its own arrival time, `run_nodes.cost` accumulates per node, and an engine-generated `usage` run-event surfaces each Action's spend in the event log. Unknown models cost 0 — a zero on the cost page means "no price entry", never a guessed price. The `node_usage` unique key includes `run_id` because session ids are node ids and repeat across runs of the same workflow.
- **Session events must be written to SQLite as they arrive.** The run SSE endpoint polls the database and there is no in-process pubsub, so `onSessionEvent` writes `run_events` rows live; batching them until the node finishes makes the run page look frozen. Every row carries `session_id` (`events.ts` from `ctx.sessionId`, `action.ts` on the `usage` event it inserts itself) — that is what puts an event on its round in the replay; the column is nullable only for rows written before ADR-0018.
- **A declared artifact that is not on disk fails the node.** The model saying it wrote the file is not evidence; this is the only mechanical backstop the dual-channel result has ([ADR-0008](docs/adr/0008-artifacts-not-values.md)).
- **`cancelled` is a terminal state distinct from `failed`** and leaves `run.error` null; a prompt that throws after cancellation is cancelled, not failed.
- **A run never stays `running`, and neither does a round.** Terminal state is written by `executeRun`, by `cancelRun`, by `failWholeRun`, and at process start by `reconcileOrphanRuns` through `src/instrumentation.ts`. Each of those five paths — plus `runner.ts`'s catch around a throwing `runActionNode` — also settles the `run_node_rounds` rows through `engine/rounds.ts`: the still-`running` rows take the matching terminal state, and every node the path bulk-skips gets one zero-duration `skipped` round. `runActionNode` opens its skeleton row (running + startedAt + inputs, snapshot filled in later) as its very first statement, ahead of anything that can throw, so "failed before the session opened" is still a segment on the timeline instead of a node stuck at 等待, and it settles that row through `settleRoundIfRunning` — cancellation can land while the Action is still awaiting `sessionOutput` / `closeSession`, and an unconditional success would overwrite the `cancelled` `cancelRun` already wrote. The reverse order needs the opposite: when the success lands first and the cancel arrives before `runOne`'s post-await check, `closeRunningRounds` finds no `running` row, so `runner.ts`'s cancel branch rewrites that round to `cancelled` **unconditionally**, in the same place it writes `run_nodes`. The two sites are deliberately opposite — a node and its round must agree on the terminal state, or the replay paints a cancelled node over a successful segment.
- **A subprocess disposal failure quarantines the run.** If `dispose()` cannot prove the child exited even after its termination escalation, keep the run in `activeRuns` and retain its process handle; file preview, cleanup, deletion, and new-run capacity must remain fail-closed until the Next process restarts and owns teardown. If both session close and process disposal are unconfirmed, keep that session's usage rollup live as well: late chunks must idempotently refresh `run_nodes` and its existing `usage` event until process exit is confirmed.
- The run SSE endpoint polls SQLite; there is no in-process pubsub, and the run stream ends only after three silent ticks past terminal state.

## Comments and documentation

A comment that records why a workaround exists is the rule itself — `longHaulFetch`, the per-workspace event pump, the `SUM` rollup, `LIKE` escaping, the quiet-tick stream ending — so deleting the comment deletes the rule. Comments state behavior, failure, timing, and ownership in Chinese; they do not narrate control flow or restate code. Every remaining `any` carries a comment naming why narrowing is infeasible. When a change alters a contract that [docs/DESIGN.md](docs/DESIGN.md) or [docs/DESIGN-V2.md](docs/DESIGN-V2.md) states, or settles a term, update that document in the same change.

## Decisions and the glossary

- [CONTEXT.md](CONTEXT.md) owns domain vocabulary and semantics only; keep implementation out of it and update it the moment a term is settled.
- [docs/DESIGN.md](docs/DESIGN.md) owns the v1 contract and the current DeepSeek Harness engine spec; [docs/DESIGN-V2.md](docs/DESIGN-V2.md) owns the list, folder, reference, revision, and shared-UI contracts.
- [README.md](README.md) is the product pitch and the only document carrying the demo walkthrough; it duplicates the startup and test commands from the Commands block above and restates behavior the engine spec owns, so change README, that block, and the spec together or change none of them.
- The two `/api/monitor/*` routes (health, cleanup) have no written contract; `src/server/monitor/` is their source of truth.
- An irreversible, surprising, genuinely traded-off decision gets an ADR at `docs/adr/NNNN-slug.md`. The three-of-three test and the numbering rule live in [ADR-FORMAT.md](.claude/skills/domain-modeling/ADR-FORMAT.md); the house shape is a Chinese title, the decision, then a `理由：` paragraph ending in the cost, as in [ADR-0005](docs/adr/0005-folders-not-tags.md).
- A superseded ADR stays in place: link the replacement from the old one and the old one from the replacement, as [ADR-0003](docs/adr/0003-tags-not-folders.md) and ADR-0005 do.
- The harness-seam decisions, in one line each: [ADR-0013](docs/adr/0013-explicit-composition-headless-baseline.md) the composition is an explicit flat list with the upstream headless session as the experience baseline (`catalog.ts` + `docs/harness/`); [ADR-0014](docs/adr/0014-model-must-not-spawn-agents.md) the model never spawns agents — the delegation family stays unmounted and `run_in_background` is off, orchestration is the graph; [ADR-0015](docs/adr/0015-node-scope-in-setup-window-not-presets.md) Action-level narrowing is registered in the session setup window, no agent presets; [ADR-0016](docs/adr/0016-three-tier-settings.md) three-tier settings (global baseline, workflow declaration, Action narrowing) plus directory Skills with preload — `src/lib/workflow-settings.ts`, the workflow settings page, `workflow_skills` / `workflow_tools` / `action_preloads` / `skill_files`, `runs.settingsSnapshot`; [ADR-0017](docs/adr/0017-tool-is-a-contract.md) a Tool is an OntoFlow contract and the cordis wrapper belongs to the platform — `tool-contract.ts` + `tool-plugin.ts`, the three seed Tools rewritten in contract form. `docs/harness/` is the review record those decisions produce; its own `AGENTS.md` owns the upstream-upgrade procedure.
- [.github/REVIEW.md](.github/REVIEW.md) is the review checklist derived from this file — the same invariants regrouped by what to look at in a diff; when a rule here changes, the matching REVIEW.md line changes in the same commit.
- Cite an ADR from code by bare id, in the comment at the line the decision constrains (`（ADR-0005）`).
- Use the [domain-modeling](.claude/skills/domain-modeling/SKILL.md) skill when changing terminology or recording a decision. `.claude/skills/` and `.codex/skills/` hold byte-identical copies of all five skills; edit both trees in the same change.
- A simplification candidate is one record under [docs/simplifications/](docs/simplifications/README.md) (`proposed` / `done` / `rejected`, skeleton pinned by `src/rules.test.ts`); the `find-simplifications` skill produces them, and a rejection's rationale stays there rather than growing this file.

## Editing these instructions

`CLAUDE.md` resolves to `AGENTS.md`; edit `AGENTS.md`. State only rules the repository already obeys, keep each rule self-contained with its rationale behind a link, and delete a rule the moment the code stops obeying it. A rule that can be checked mechanically also lives in `src/rules.test.ts`, and a rule a reviewer must check by eye has a line in `.github/REVIEW.md`; change the three together. A decision that is settled but not yet implemented is written as such — "decided, see ADR-NNNN, second batch" — the way the `CONTEXT.md` header note does, never as if the code already did it. The `nextjs-agent-rules` block below is rewritten by `next dev`; commit it with your work instead of deleting it from the diff.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
