# CONTEXT

OntoFlow 工作台的领域术语表（ubiquitous language）。只记语义，不记实现。

> **Action** 与 **Action 模板** 两条记的是已经定下、但代码还没搬过去的模型
> （[ADR-0010](docs/adr/0010-nodes-own-their-definition.md)）：库里的 Action 目前
> 仍被工作流节点共享引用，节点还不自带定义。这次搬迁尚未排进里程碑。

## Language

### 库与实体

**库（Library）**:
按实体种类划分的集合：工作流、Action 模板、Skill、Tool、对象类型；库里的一个成员称作实体。
_Avoid_: 可管理实体

**Action**:
工作流图上的一个 agent 节点：一次有名字的模型工作。Action 自带自己的完整定义——任务、
规则、端口、出口端口、产物、模型、思考强度——并归它所在的那个工作流所有，不被别的工作流共享。
_Avoid_: 动作, agent 节点

**Action 模板**:
库里可复制的 Action 定义原型。把模板放上画布得到一个 Action，此后两者再无关系：
改模板不影响已经放下的 Action，改 Action 也不回写模板。
_Avoid_: 共享 Action, 可复用 Action

**Skill**:
独立管理的命名技能：一份带名字与描述的指令包。运行时物化进工作区的技能目录，
由模型看描述自行决定要不要加载——它是可用能力，不是强制规范。必须遵守的东西写进
Action 自己的规则或工作流级共同指令。
_Avoid_: 技能, 强制注入的 prompt 片段

**Tool**:
模型在 Action 执行时可调用的一段自定义能力，独立管理，只对声明了它的 Action 可见。
_Avoid_: 工具, MCP 服务器

**对象类型（Object Type）**:
产物的契约类型：注册表里一个有名字的类型，如 `岗位JD文件`、`简历Markdown`、`评委结论`。
它回答"这个端口进出的产物是什么"。约束只在编辑期成立——连线时同名类型才连得上，运行时
没有任何机制去核对文件内容真的符合该类型。
_Avoid_: 类型, 端口类型, 值类型

### 编排

**工作流（Workflow）**:
由节点和连线构成的有向图，描述一组 Action 之间的编排。图允许回边，因此不是 DAG。
_Avoid_: DAG, 有向无环图

**节点（Node）**:
工作流图上的一个点，三种之一：Action 节点、输入节点、输出节点。

**端口（Port）**:
节点上一个有名字的输入位或输出位，绑定一个对象类型。
_Avoid_: handle

**出口端口（Exit Port）**:
Action 节点上具名的输出分支。节点执行完毕后按自己的数据面结果选中其中一个出口，
只有被选中的那个出口上的连线才激活——"评审通过还是打回"这类判定就住在这里。
_Avoid_: 条件边, 分支节点, branch

**连线（Edge）**:
从上游节点的出口端口到下游节点的输入端口的连接；只有绑定同一个对象类型的端口之间才连得上。
连线是哑的：不带条件、不做转换，只表达"下游要读上游的这份产物"，并据此生成下游提示里
那句"去读哪个文件"。
_Avoid_: 边, 线, Connection

**扇出（Fan-out）**:
一个出口端口连出多条线，下游多个 Action 并行执行。扇出是图的形状，不是一种节点类型。
_Avoid_: 并行节点, fan-out 节点

**汇总（Synthesis）**:
一个输入端口接进多条线的 Action：它读齐全部上游产物再产出。汇总同样是图的形状，不是一种节点类型。
_Avoid_: 聚合节点, synthesis 节点

**循环（Loop）**:
出口端口连回上游 Action 形成的回边。被回流的那个 Action 自己声明最多重入几次，
以及次数耗尽时是按失败收束还是按接受收束。
_Avoid_: review-fix 节点, 迭代节点

**轮次（Round）**:
循环中某个 Action 的第几次执行。每一轮都是全新的会话：上一轮的产物与评审意见经工作区
文件传给下一轮，不靠会话记忆延续。

**输入节点（Input Node）**:
工作流的入口内置节点：绑定一个对象类型，其值由发起运行的人提供。一切输入以文件落盘
进工作区，是这次运行最初的产物：文件类原件照存，文字与 JSON 物化为文件。不做任何
预处理，格式转换是读它的 Action 的工作；要读它的 Action 从它拉一条线。

**输出节点（Output Node）**:
标记工作流级最终产出的内置节点。

### 执行

**运行（Run）**:
一次工作流执行及其完整历史记录。
_Avoid_: 执行, 跑

**工作区（Workspace）**:
一次运行独占的目录，是这次运行里全部 Action 的共同工作场所，也是它们之间唯一的交流场所。
运行之间的工作区互不可见。
_Avoid_: 运行目录, 沙箱

**产物（Artifact）**:
Action 写进工作区的文件，承载它工作成果的实质内容。下游 Action 读它，人也读它。
_Avoid_: 输出文件, 中间结果

**双通道结果（Dual-channel Result）**:
Action 每次执行同时交出两样东西：**数据面**是结构化输出，只放下游用来路由与判定的少数
字段；**上下文面**是产物文件，放全部实质内容。实质内容不进数据面，也不沿连线复制进下游
提示；下游只拿到工作区里的产物路径，并按需读取文件。
_Avoid_: 双输出, 结构化输出（单指数据面时才用这个词）

**已取消（Cancelled）**:
由人主动终结而结束的运行或节点终态，区别于出错导致的失败。
_Avoid_: 中止

**会话（Session）**:
一个 Action 执行一轮时独占的一次模型会话。
_Avoid_: 对话

**Agent 轨迹（Agent Trajectory）**:
一个 Action 某轮会话的可检查过程记录，按回合与步骤组织输入、模型回复和工具调用；它回答
“模型实际怎样完成这一轮”，不同于记录配置的运行快照和跨节点的实时摘要。
_Avoid_: 对话日志, 运行快照, 事件日志

**思考强度（Reasoning Effort）**:
Action 执行时投入的推理深度档位。
_Avoid_: 强度, 思考, variant

**运行快照（Run Snapshot）**:
一次运行中某个 Action 实际使用的完整配置的冻结副本，外加这次运行导入的每一项技能与
工具的内容摘要。技能与工具本身不冻结——工作区里是指向全局库活目录的链接——所以摘要
能证明"跟那次是不是同一份"，但不保证能把旧内容取回来。

### 治理

**引用（Reference）**:
一个实体在自己的定义里用到另一个实体的关系。会引用别人的只有两种：工作流（经由它自己的
Action）与 Action 模板；它们两个自己都从不被引用。

**修订（Revision）**:
库实体某一时刻定义的完整历史副本——"这个实体一路怎么变过来的"的答案。
_Avoid_: 版本, 版

**文件夹（Folder）**:
Action 模板 / Skill / Tool / 对象类型四个库共享的单归属归类位置：一个实体最多属于一个
文件夹，"它在哪"有唯一答案。
_Avoid_: 标签, 流程树
